{
  "name": "council-of-errors",
  "version": "1.0.0",
  "description": "A telegram relay bot for error messaging",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server.js --ignore local_db.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dockerode": "^4.0.9",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
